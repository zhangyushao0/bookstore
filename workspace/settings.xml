<settings xmlns="http://maven.apache.org/SETTINGS/1.0.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.0.0 https://maven.apache.org/xsd/settings-1.0.0.xsd">
  <servers>
    <server>
      <id>github</id> <!-- This should match the id in your pom.xml's distributionManagement section -->
      <username>zhangyushao0</username> <!-- YOUR_GITHUB_USERNAME -->
      <password>${env.GITHUB_TOKEN}</password> <!-- This uses the GITHUB_TOKEN secret for authentication -->
    </server>
  </servers>
</settings>
